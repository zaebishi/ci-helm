{{- if and .Values.envFrom.enabled .Values.envFrom.secretYaml }}
apiVersion: v1
kind: Secret
metadata:
  {{- $secDefault := printf "%s-secret" .Release.Name }}
  name: {{ .Values.envFrom.secretName | default $secDefault }}
type: Opaque
stringData:
{{ .Values.envFrom.secretYaml | nindent 2 }}
{{- end }}