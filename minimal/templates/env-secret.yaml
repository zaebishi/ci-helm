{{- if and .Values.envFrom.enabled .Values.envFrom.secretYaml }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ default (printf "%s-secret" (include "chart.fullname" .)) .Values.envFrom.secretName }}
type: Opaque
stringData:
{{ .Values.envFrom.secretYaml | nindent 2 }}
{{- end }}